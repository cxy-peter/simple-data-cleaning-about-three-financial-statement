# 三表核对

Skill ID：`statement_review`；版本：1。

## 触发与参数

触发词：三表、三大报表、核对、资产负债。
工具白名单：snapshot、reconcile、brief。
检索上限 top-k：4。
追加查询词：三大报表 单位 合并 口径。

## 执行约束

财务任务必须给公司和年度；比较必须至少两家公司、同一年度。缺数不借用其他年份。数字由程序计算，检索仅限有效授权资料。原文不足时不让模型补造。

## 输出与反馈

返回程序材料、来源、执行 Trace 和待核实事项。不发邮件、不付款、不修改 ERP。用户反馈进入人工待办，不自动发布 Skill。

## 实现位置

`qiming/engine.py` 的 `SKILLS` 与 `Engine.ask` 是运行逻辑；本 Markdown 是同步说明，不会被当任意代码执行。修改时需同时更新代码与说明，再运行 tests 和 evaluate.py。
